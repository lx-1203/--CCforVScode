# 备份文件
**/*.bak

# 补丁/临时脚手架脚本（构建期使用，不进入发布包）
_*.js

# 源码映射与开发杂项
**/*.map
.gitignore
.vscode/**

# CodeKey npm 开发依赖（不要打进 vsix）
codekey/node_modules/.package-lock.json
codekey/node_modules/.bin/**
codekey/package.json
codekey/package-lock.json
