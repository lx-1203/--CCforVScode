/* PWA mode — no Telegram start param support. Stub for compatibility. */
export function getTelegramStartParam(_params: URLSearchParams): string | null { return null; }
export function parsePairingStartParam(_startParam: string | null): { code?: string; keyId?: string; contentKey?: string; } | null { return null; }
