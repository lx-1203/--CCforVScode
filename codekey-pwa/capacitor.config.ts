import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xingji.codekey',
  appName: '星迹CodeKey',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
